@Library('linkurious-shared@feature/hadolint-icon')_

dockerJob {
  // General
  projectName = "linkurious/docker-lke"
  podTemplateNames = ['jnlp-agent']
  buildkitBuilder = true

  // helmReleaseBranch = "develop"

  createGitTag = true
  runBookeeping = true
  githubRelease = true
}
